ESP32 智能服药提醒与激励系统
一、硬件配置（工程清单）
主控模块：ESP32 开发板
输出设备：0.96OLED、7 段数码管、舵机、无源蜂鸣器
输入设备：物理按键
外设功能：预留服药合盖检测接口、数据库通信接口
________________________________________
二、核心功能需求（工程化定义）
1. 7 段数码管显示模块
•	功能定义：实现指定十进制数字的静态 / 持续显示；
•	显示逻辑：接收主控下发的数值参数，驱动数码管稳定输出对应数字；
•	精度要求：无闪烁，数字显示清晰无乱码。
2. oLED显示模块（双模式）
模式 1：激励表情显示模式
•	功能定义：内置两组预设图案（正向激励表情 / 负向提示表情）；
•	触发逻辑：根据用户服药行为，主控切换对应图案全屏显示。
模式 2：开盖方位指引模式
•	功能定义：系统检测到开盖动作后，点阵自动切换为4 分区显示（分割为左上、右上、左下、右下 4 个独立 4×4 子灯阵）；
•	功能逻辑：高亮指定分区，为用户指示目标服药区域。
3. 舵机执行模块（开盖控制）
•	功能定义：舵机接收 PWM 信号，旋转至预设角度实现药盒开盖；
•	控制逻辑：双条件联锁触发（缺一不可）：
1.	到达系统预设的定时服药时间窗；
2.	用户按下物理按键触发有效信号；
•	动作要求：定时时间内仅执行一次开盖动作，角度精准无抖动。
4. 无源蜂鸣器模块（双音效提醒）
•	音效 1：服药定时提醒 → 固定频率间歇声响，用于提醒用户服药；
•	音效 2：正向激励提醒 → 特定频率提示音，用户完成合规服药后触发；
•	控制逻辑：由主控根据系统状态独立触发，互不干扰。
5. 物理按键模块
•	功能定义：作为开盖动作的使能触发信号；
•	逻辑约束：仅在定时服药时间窗内，按键信号有效；非定时时间内，按键无响应。
________________________________________
三、系统核心联锁逻辑（硬性约束）
1.	舵机开盖：仅允许「预设定时时间 + 按键按下」双条件满足时执行；
2.	点阵分区指引：仅在开盖状态下激活，关盖后自动切换回激励表情模式；
3.	蜂鸣器激励音：仅在检测到「用户按时服药 + 药盒合盖」后触发；
4.	显示设备优先级：点阵屏优先响应开盖 / 服药状态，数码管独立显示数值互不干扰。
________________________________________
四、扩展开发需求（预留接口，对应你的「下一步」规划）
1.	通信扩展：ESP32 集成网络功能，对接云端数据库，实时拉取远程下发的数码管显示数值；
2.	传感扩展：接入合盖检测传感器，自动识别服药完成状态，作为激励表情、蜂鸣器的触发依据；
3.	功能扩展：支持定时参数、显示图案、音效参数的远程配置。
